import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  output: "standalone",
  allowedDevOrigins: ["backward-asparagus-ladybug.ngrok-free.dev"],
};

export default nextConfig;
